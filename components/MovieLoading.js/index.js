import { View } from "react-native";

export const MovieLoading = () => {
  return (
    <View
      style={{
        flexDirection: "column",
        width: 175,
        height: 318,
        backgroundColor: "#2a2a2a",
        borderRadius: 8,
        elevation: 8,
      }}
    ></View>
  );
};
